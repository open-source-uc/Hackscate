---
// Animated gradient mesh background with grain effect
---

<div class="animated-background">
    <div class="gradient-mesh"></div>
</div>

<style>
    .animated-background {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: -1;
        overflow: hidden;
        background: var(--background);
    }

    .gradient-mesh {
        position: absolute;
        top: -50%;
        left: -50%;
        width: 200%;
        height: 200%;
        background: radial-gradient(
                circle at 20% 50%,
                oklch(0.6304 0.2475 29.29) 0%,
                oklch(0.9098 0.1728 100.22) 8%,
                oklch(0.9753 0.0434 98.65) 12%,
                oklch(0.8374 0.0935 213) 16%,
                oklch(0.3987 0.2591 264.4) 20%,
                transparent 25%
            ),
            radial-gradient(
                circle at 80% 80%,
                oklch(0.9098 0.1728 100.22) 0%,
                oklch(0.9753 0.0434 98.65) 8%,
                oklch(0.8374 0.0935 213) 12%,
                oklch(0.3987 0.2591 264.4) 16%,
                transparent 22%
            ),
            radial-gradient(
                circle at 40% 20%,
                oklch(0.8374 0.0935 213) 0%,
                oklch(0.3987 0.2591 264.4) 8%,
                oklch(0.6304 0.2475 29.29) 12%,
                transparent 18%
            ),
            radial-gradient(
                circle at 60% 90%,
                oklch(0.3987 0.2591 264.4) 0%,
                oklch(0.6304 0.2475 29.29) 8%,
                oklch(0.9098 0.1728 100.22) 12%,
                transparent 18%
            ),
            var(--background);
        animation: meshMove 20s ease-in-out infinite;
        filter: blur(80px);
        opacity: 0.3;
    }

    .grain-overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 400 400' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='1.5' numOctaves='6' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
        opacity: 0.08;
        mix-blend-mode: overlay;
        pointer-events: none;
    }

    @keyframes meshMove {
        0% {
            transform: translate(0, 0) rotate(0deg);
        }
        33% {
            transform: translate(-5%, 10%) rotate(120deg);
        }
        66% {
            transform: translate(5%, -5%) rotate(240deg);
        }
        100% {
            transform: translate(0, 0) rotate(360deg);
        }
    }

    /* Respect user's reduced motion preference */
    @media (prefers-reduced-motion: reduce) {
        .gradient-mesh {
            animation: none;
        }
    }
</style>

---
import { Button } from "@/components/ui/button";

interface Props {
    lang: string;
    t: ReturnType<typeof import("@/i18n/utils").useTranslations>;
}

const { lang, t } = Astro.props;
const e = t("explainer");
---

<section class="flex flex-col items-center justify-center py-48">
    <h1 class="text-[10rem] font-bold font-width-wide text-foreground py-4">
        HACKSCATE
    </h1>
    <p class="text-lg text-muted-foreground">
        {e.p1}
    </p>
    <div class="flex flex-row items-center pt-12 gap-4">
        <a href={`/${lang}`} tabindex="-1">
            <Button variant="hackscate" size="hackscate_lg">{e.join}</Button>
        </a>
        <a href={`/${lang}`} tabindex="-1">
            <Button variant="hackscate_outline" size="hackscate_lg">{e.learn_more}</Button>
        </a>
    </div>
</section>

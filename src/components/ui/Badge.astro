---
interface Props {
    className?: string;
}

const { className = "" } = Astro.props as Props;
---

<span class={`bg-card-foreground text-card py-1.5 px-5 rounded-tl-2xl rounded-br-2xl ${className}`}>
    <slot />
</span>
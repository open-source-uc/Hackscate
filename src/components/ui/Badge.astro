---
interface Props {
    className?: string;
}

const { className = "" } = Astro.props as Props;
---

<span class={`bg-white text-black py-1 px-4 rounded-tl-2xl rounded-br-2xl ${className}`}>
    <slot />
</span>